# STM32 学习笔记 📝

> 📅 2026-07-20 创建 | 正点原子 HAL 库教程笔记
> 老大口述，我来记录整理

---

## 第1讲 单片机简介

<!-- 待记录 -->

---

## 第2讲 Cortex-M系列介绍

<!-- 待记录 -->

---

## 第3讲 初识STM32

<!-- 待记录 -->

---

## 第4讲 学会查看数据手册

<!-- 待记录 -->

---

## 第5讲 最小系统和IO分配

<!-- 待记录 -->

---

## 第6讲 常用开发工具简介 & 安装MDK

<!-- 待记录 -->

---

## 第7讲 安装仿真器驱动 & USB虚拟串口驱动

<!-- 待记录 -->

---

## 第8讲 MDK5编译例程和串口下载程序

<!-- 待记录 -->

---

## 第9讲 DAP下载程序及程序调试

<!-- 待记录 -->

---

## 第10讲 文本美化 & 代码编辑技巧

<!-- 待记录 -->

---

## 第11讲 查找替换 & 编译问题定位 & 窗口视图管理

<!-- 待记录 -->

---

## 第12讲 C语言基础知识复习1

### 宏定义用 `do { ... } while(0)` 包裹

多语句宏必须用 `do { ... } while(0)` 包起来，否则会有隐患：

```c
// ❌ 错误写法：直接裸写多条语句
#define SET_LED(value)   GPIOA->ODR = value; GPIOB->ODR = value

// 用在 if 后面就炸了：
if (condition)
    SET_LED(0x01);   // 展开后只有第一句在 if 里，第二句无条件执行！
```

```c
// ✅ 正确写法：用 do-while(0) 包住
#define SET_LED(value)   do { \
                            GPIOA->ODR = value; \
                            GPIOB->ODR = value; \
                          } while(0)

// 展开后就是一个完整的语句块，if 后面不会出问题
```

**为什么用 `do-while(0)` 而不是直接花括号？**

```c
// 用花括号也能包住多语句，但有个小问题：
#define SET_LED(value)   { GPIOA->ODR = value; GPIOB->ODR = value; }

// 这种情况会多一个分号导致编译错误：
if (condition)
    SET_LED(0x01);   // 展开变成 { ... };  ← 多出来的分号
else
    do_something();  // 编译报错！else 找不到 if
```

```c
// do-while(0) 完美解决：它需要一个分号结尾，语法上刚好匹配
if (condition)
    do { ... } while(0);  // ← 分号恰好是 do-while 需要的
else
    do_something();       // ✅ 正常
```

> 📌 HAL 库里大量使用这种写法，看到 `do { ... } while(0)` 就知道是宏定义技巧。

---

## 其他 / 踩坑记录

<!-- 待记录 -->

---

> 📝 老大，随时告诉我你要记的内容，告诉我是哪一讲的，我来整理到这里。
